NOTE ON VERSIONING

To maintain versions:

  1. Edit file header for next version (include description)

  2. Complete code changes.

  3. Copy file into "previous versions" subdirectory, rename with version
     number.

6/20/2019

This directory on M: is now a git repo. So far I have created one clone of it, on
    laddie:/e/Automatic_Blinds (local git repo)/Blinds_Control
   
     
